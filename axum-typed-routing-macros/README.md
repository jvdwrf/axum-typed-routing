# axum-typed-routing-macros

The procedural macros behind [`axum-typed-routing`](https://crates.io/crates/axum-typed-routing).

Don't depend on this crate directly. Use `axum-typed-routing`, which re-exports these macros together with the router traits they need. See its [documentation](https://docs.rs/axum-typed-routing).
